const initialState = {
  todayDate: null,
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

let date = new Date(2020, 0, 20);
console.log(date);

export default reducer;
