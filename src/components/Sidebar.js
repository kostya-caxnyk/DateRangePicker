import React from 'react';
import SidebarButtons from '../containers/SidebarButtonsContainer';

const Sidebar = () => {
  return (
    <div className="sidebar">
      <SidebarButtons />
    </div>
  );
};

export default Sidebar;
